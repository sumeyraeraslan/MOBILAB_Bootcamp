import 'package:flutter/widgets.dart';

class FavIconView extends StatelessWidget {
  const FavIconView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Favoriler'),
    );
  }
}